# Superseded / dead code — kept for reference only, NOT in the live pipeline.
See ../V2-STATUS.md for the eval history. derive_predicates.py was the 55%-FP graph-shortest-path engine; client-join.py the regex route double-extractor; both replaced by actor-model.py + AST facets.
